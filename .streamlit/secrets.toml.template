# Streamlit Cloud Secrets
# In Streamlit Cloud: go to App Settings → Secrets → paste the block below (without this comment line)
# Do NOT commit the actual secrets.toml file — only this template.
# Replace YOUR_KEY_HERE with your real ICA Codex API key.

ICA_API_KEY = "YOUR_KEY_HERE"
ICA_BASE_URL = "https://api.nextgen-beta.ica.ibm.com/ica/v1"
ICA_MODEL = "gpt-5.5-gus"
ICA_TEMPERATURE = "1.0"
ICA_MAX_TOKENS = "8192"

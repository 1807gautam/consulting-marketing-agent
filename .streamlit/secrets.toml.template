# Streamlit Cloud secrets template
# In Streamlit Cloud: go to App Settings → Secrets and paste these values (without the .template extension)
# NEVER commit the real secrets.toml to git.

ICA_BASE_URL = "https://api.nextgen-beta.ica.ibm.com/ica/v1"
ICA_API_KEY = "your_ica_api_key_here"
ICA_MODEL = "claude-sonnet-4-5"
ICA_MAX_TOKENS = "8192"
ICA_TEMPERATURE = "0.3"
